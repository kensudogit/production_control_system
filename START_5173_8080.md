# ポート5173と8080を起動する方法

## 🔴 現在の状況

- **ポート5173**（Vite開発サーバー）: 起動していない
- **ポート8080**（API Gateway）: 起動していない（TIME_WAIT状態のみ）

## ✅ 解決方法

### 方法1: Docker Composeで全サービスを起動（推奨）

#### ステップ1: Docker Desktopを起動

1. **Docker Desktopを起動**
   - タスクバーにDockerアイコンがあるか確認
   - 起動していない場合は、Docker Desktopを起動
   - 起動完了まで1-3分待つ（アイコンが緑色になるまで）

2. **PowerShellを管理者として実行**
   - Windowsキーを押す
   - 「PowerShell」と入力
   - 「Windows PowerShell」を右クリック
   - 「管理者として実行」を選択

#### ステップ2: 全サービスを起動

```powershell
cd C:\devlop\production_control_system

# 全サービス起動スクリプトを実行
.\start-all.ps1
```

または、手動で起動：

```powershell
cd C:\devlop\production_control_system

# 1. 既存のコンテナを停止・削除
docker-compose down

# 2. データベースとRedisを先に起動
docker-compose up -d postgres redis

# 3. データベースの準備を待つ（15秒）
Start-Sleep -Seconds 15

# 4. API Gatewayを起動
docker-compose up -d api-gateway

# 5. API Gatewayの準備を待つ（10秒）
Start-Sleep -Seconds 10

# 6. Auth Serviceを起動
docker-compose up -d auth-service

# 7. フロントエンドを起動
docker-compose up -d --build frontend

# 8. 状態を確認
docker-compose ps
```

これで以下が起動します：
- **ポート8080**: API Gateway
- **ポート3000**: フロントエンド（Docker）

### 方法2: 開発モードで起動（ポート5173）

Dockerに問題がある場合、開発モードで起動できます：

#### ステップ1: バックエンドサービスを起動（Docker）

```powershell
cd C:\devlop\production_control_system

# データベース、Redis、API Gatewayのみ起動
docker-compose up -d postgres redis api-gateway auth-service
```

#### ステップ2: フロントエンドを開発モードで起動

**新しいPowerShellウィンドウを開いて**：

```powershell
# フロントエンドディレクトリに移動
cd C:\devlop\production_control_system\frontend

# 依存関係をインストール（初回のみ）
npm install

# 開発サーバーを起動
npm run dev
```

これで **http://localhost:5173** でアクセスできます。

**注意**: 
- この方法では、バックエンドAPI（http://localhost:8080）が起動している必要があります
- 開発サーバーは別のPowerShellウィンドウで実行します

## 🔍 確認方法

### ポート8080の確認

```powershell
# API Gatewayの状態確認
docker-compose ps api-gateway

# API Gatewayのログ確認
docker-compose logs api-gateway

# ポート8080がリッスンしているか確認
netstat -ano | findstr :8080

# ブラウザで確認
# http://localhost:8080/actuator/health
```

### ポート5173の確認

```powershell
# ポート5173がリッスンしているか確認
netstat -ano | findstr :5173

# ブラウザで確認
# http://localhost:5173
```

## 🛠️ トラブルシューティング

### 問題1: Dockerコマンドが実行できない

**症状**: `Access is denied` エラー

**解決策**:
1. Docker Desktopを起動
2. PowerShellを管理者として実行
3. Docker Desktopを再起動

### 問題2: ポート8080が起動しない

**確認**:
```powershell
# API Gatewayのログを確認
docker-compose logs api-gateway

# データベースが起動しているか確認
docker-compose ps postgres
```

**解決策**:
```powershell
# API Gatewayを再起動
docker-compose restart api-gateway

# または、再ビルドして起動
docker-compose up -d --build api-gateway
```

### 問題3: ポート5173が起動しない

**確認**:
```powershell
# フロントエンドディレクトリに移動
cd C:\devlop\production_control_system\frontend

# 依存関係がインストールされているか確認
Test-Path node_modules

# インストールされていない場合
npm install

# 開発サーバーを起動
npm run dev
```

**エラーが出る場合**:
- Node.jsがインストールされているか確認: `node --version`
- npmがインストールされているか確認: `npm --version`
- ポート5173が使用されていないか確認: `netstat -ano | findstr :5173`

### 問題4: API Gatewayに接続できない

**確認**:
```powershell
# API Gatewayのヘルスチェック
curl http://localhost:8080/actuator/health

# または、ブラウザで
# http://localhost:8080/actuator/health
```

**解決策**:
1. データベースが起動しているか確認
2. API Gatewayのログを確認
3. API Gatewayを再起動

## 📋 チェックリスト

- [ ] Docker Desktopが起動している
- [ ] PowerShellを管理者として実行している
- [ ] `docker --version` が正常に動作する
- [ ] `docker ps` が正常に動作する
- [ ] データベースが起動している（`docker-compose ps postgres`）
- [ ] API Gatewayが起動している（`docker-compose ps api-gateway`）
- [ ] ポート8080がリッスンしている（`netstat -ano | findstr :8080`）
- [ ] ポート5173がリッスンしている（開発モードの場合）（`netstat -ano | findstr :5173`）

## 🚀 クイックスタート

### 全サービスを起動（Docker）

```powershell
cd C:\devlop\production_control_system
.\start-all.ps1
```

### 開発モードで起動

**PowerShellウィンドウ1（バックエンド）**:
```powershell
cd C:\devlop\production_control_system
docker-compose up -d postgres redis api-gateway auth-service
```

**PowerShellウィンドウ2（フロントエンド）**:
```powershell
cd C:\devlop\production_control_system\frontend
npm install
npm run dev
```

## 🎯 アクセス情報

起動後、以下のURLにアクセスできます：

- **フロントエンド（Docker）**: http://localhost:3000
- **フロントエンド（開発モード）**: http://localhost:5173
- **API Gateway**: http://localhost:8080
- **API Gateway ヘルスチェック**: http://localhost:8080/actuator/health
- **Auth Service**: http://localhost:8087
